# System Info Reporter

This CMD script generates a system information report and saves it as `SystemReport.txt`.

## Features

- Captures operating system, CPU, memory, and disk information.
- Saves report with a timestamp.

## How to Use

1. Download `system_info.bat`.
2. Open Command Prompt and navigate to the script folder.
3. Run the script:

```cmd
system_info.bat
```